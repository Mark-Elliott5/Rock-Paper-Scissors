function getComputerChoice() {
    let weapon = ['Rock', 'Paper', 'Scissors'];
    let computerChoice = '';
    let indexNumber = Math.floor(Math.random()*3)+1
    return computerChoice = weapon[indexNumber]
}